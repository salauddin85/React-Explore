import React from 'react';

const Blog = ({blog}) => {
    console.log(blog);
    const {id,cover,title,author_img,author,reading_time,hashtags}=blog
    return (
        <div>
            
            
        </div>
    );
};

export default Blog;